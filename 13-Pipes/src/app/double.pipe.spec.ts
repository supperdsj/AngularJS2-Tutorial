/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DoublePipe } from './double.pipe';

describe('Pipe: Double', () => {
  it('create an instance', () => {
    let pipe = new DoublePipe();
    expect(pipe).toBeTruthy();
  });
});
